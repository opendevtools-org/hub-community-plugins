# Merge into the site `.env` (do not commit secrets).

STREMIO_TAG=latest
# Host port when running this compose without Hub port override.
# STREMIO_PORT=8080

# Bind mount for server cache and config (default ./data/stremio on the site folder).
# STREMIO_DATA_DIR=./data/stremio
